<div class="column has-text-centered">
    <h1 class="subtitle is-3" style="color: #f14999;">Welcome to giveaways</h1>
        <h2><strong>Go ahead, find giveaways!</h2><h2>They're all free</strong></h2><br>
            <div id="hero-input-group" class="field has-addons has-addons-centered">
                <div class="control">
                    <input class="input is-medium" type="text" placeholder="Search by location..." />
                </div>
                <div class="control">
                    <a class="button is-medium is-info">
                        <i class="fas fa-search-location"></i>
                    </a>
                </div>
            </div>
            <h3><%= link_to 'OR GIVE SOMETHING YOUR OWN!', new_giveaway_path %></h3><br>
            <div>
                <p>There's an unwritten rule, the receiver should organize the transport of the giveaway he/she chooses at his/her expense
                <p>It should not be a burden to the giver, unless you arrange it differently</p>
            </div><br>

            <div class="section has-text-left">
              <h2 class="subtitle" style="margin-left: 30px">Featured Articles</h2>
            </div>
  <div class="columns is-mobile is-multiline is-centered">
    <% @giveaways.each do |giveaway|%>
      <div class="box has-background-white-ter" style="margin-bottom: 24px">
        <%= link_to giveaway_path(giveaway), class:"card" do %>
            <p class="bd-notification is-primary">
                <small><%= giveaway[:created_at].strftime("%m/%d/%Y") %></small><br>
                <code class="html"><small><%= giveaway.location %></small></code><br>
                <h2><%= giveaway.title.truncate(20)%></h2>
                <% unless giveaway.pictures.empty? %>
                    <div class="img image is-128x128">
                        <%= image_tag(giveaway.pictures.first.image) %>
                    </div>
                <%end%>
            </p>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
      
<%== pagy_bulma_nav(@pagy) unless @giveaways.empty? %>
