<div class="container">
  <div class="columns is-multiline">
    <div class="column is-6 is-offset-3">
      <div class="content is-small">
        <h2><%= @giveaway.title %></h2>
      </div>
    </div>
    <div class="column is-6 is-offset-3">
      <div class="card">
        <div class="card-content">
          <div class="content">
            <p><%= @giveaway.description %></p>
            <code class="html"><small><%= @giveaway.location %></small></code>
          </div>
        </div>

          <div class="image is-256x256">
            <%= image_tag("/assets/212-2127438_decent-giveaways-png.png", class:"img-responsive") %><br>
          </div>
  
        <footer class="modal-card-foot" style="justify-content: space-between">
          <%= link_to 'Back', giveaways_path, class: "button is-light" %>
<<<<<<< HEAD
          <p style="margin-left:0.5em">
            <%= link_to edit_giveaway_path(@giveaway) do %>
                <i class="fas fa-edit fa-2x"></i>
          </p>
          <% end %>
          <p style="margin-left:0.5em">
            <%= link_to giveaway_path(@giveaway), method: :delete,
              data: { confirm: 'Are you sure you want to delete this giveaway?'} do %>
              <i class="far fa-trash-alt fa-2x"></i>
            <% end %>
          </p>
          <% if logged_in? %>
            <p>
              <%= link_to 'New Comment', new_giveaway_comment_path(@giveaway) %>
            </p>
          <% end %>
=======

          <% if logged_in? && current_user == @giveaway.user%>
            <p style="margin-left:0.5em">
              <%= link_to edit_giveaway_path(@giveaway) do %>
                  <i class="fas fa-edit fa-2x"></i>
              <% end %>
            </p>

            <p style="margin-left:0.5em">
              <%= link_to giveaway_path(@giveaway), method: :delete,
                data: { confirm: 'Are you sure you want to delete this giveaway?'} do
              %>
              <i class="far fa-trash-alt fa-2x"></i>
              <% end %>
            </p>
          <% end %>

          <p>
            <%= link_to 'New Comment', new_giveaway_comment_path(@giveaway) %>
          </p>
>>>>>>> 60bc2aab97548d57160f1de8e63f83ff2d6fa612
        </footer>
      </div>
    </div>
  </div>
</div>

<% @giveaway.comments.each do |comment| %>
  <%= render comment %>
<% end %> 
